#include <iostream>
using namespace std;

int main() {

	char str[80];
	char* p1;

	// p1 is assigned the address of the first element, i.e. str[0], in the str array.
	p1 = str;
	// That is, after the assignment, p1 will point to str[0]. (p1 will contain str[0]'s address.)
}