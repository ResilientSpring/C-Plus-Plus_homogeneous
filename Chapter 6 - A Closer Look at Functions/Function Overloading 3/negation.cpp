// Create various versions of the neg() function.

#include <iostream>
using namespace std;

int neg(int n);
double neg(double n);
long neg(long n);


int main() {
	
}

// neg() for int.
int neg(int n) {
	return -n;
}

// neg() for double.
double neg(double n) {
	return -n;
}

// neg() for long.
long neg(long n) {
	return -n;
}
