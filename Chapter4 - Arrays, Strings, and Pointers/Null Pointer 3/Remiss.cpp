#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <iomanip>
#include <string.h>

int main() {
	// In a declaration statement, preceding a variable name with a * causes that variable (p) to be a pointer.
	float *p;     // A pointer is a variable/object that can hold a memory address (of the specified type).
	// ----- End of declaration statements.    Operation starts

	p = NULL;


}