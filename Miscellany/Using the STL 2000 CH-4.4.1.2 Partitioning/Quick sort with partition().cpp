#include <iostream>
#include <algorithm>
#include <functional>
using namespace std;


void QuickSort(int *first, int *last) {

	int len;
	int *pivot, *second, *splitPoint;

	len = distance(first, last);
}


int main() {



}