#include <iostream>
#include <algorithm>
#include <functional>
using namespace std;


void QuickSort(int *first, int *last) {

	int len;
	int *pivot, *second, *splitPoint;

	len = distance(first, last);

	if (len < 2)
		return;

	if (len == 2) {
		second = first;
		second++;
	}

}


int main() {



}